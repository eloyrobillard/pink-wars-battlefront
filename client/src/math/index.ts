import { lerp } from './lerp';
import { randRot, randPos, randDir } from './random';

const math = { 
  lerp, 
  cos: Math.cos, 
  sin: Math.sin, 
  randRot, 
  randPos,
  randDir
};

export default math;