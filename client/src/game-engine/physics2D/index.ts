import { vec2 } from '../types/index';

/**
 * @param  {vec2} origin screen point from where the ray is cast
 * @param  {vec2} direction
 * @param  {number} distance distance from origin until when to cast ray
 * @returns vec2
 */
function Raycast (origin: vec2, direction: vec2, distance: number = Infinity): vec2 {

}

const Physics2D = {};

export default Physics2D;
